# Packaged Python runtime

This directory is populated at release-build time by
`scripts/vendor-python-runtime.py`. Generated packages are intentionally not
committed; their installed metadata and licenses are included in the app
bundle.
